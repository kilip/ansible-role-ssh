---
ssh_key_files:
  - id_rsa
ssh_key_user: "root"
ssh_key_group: "{{ ssh_key_user }}"
ssh_key_type: rsa
ssh_key_size: 4096
ssh_key_dir: "/home/{{ ssh_key_user }}/.ssh"
ssh_key_passphrase: ""
ssh_key_force: false
ssh_backup_dir: "files/ssh-keys/"
